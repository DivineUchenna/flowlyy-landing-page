# Flowlyy landing page

Static HTML, CSS and a little JavaScript. No build step.

- `index.html`: page markup
- `styles.css`: all styles
- `script.js`: product filters, ROI calculator, demo window tabs, email form handlers
- `assets/`: logos, divider pattern and founder photo

Open `index.html` in a browser to preview.

## To do before launch

- Point the email forms at a real endpoint (see the TODO comments in `index.html` and `script.js`).
- Replace the `#` social links in the footer with real profile URLs.
